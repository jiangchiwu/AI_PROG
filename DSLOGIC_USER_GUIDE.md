
# DSlogic DScope 使用指南

## 概述

DSlogic DScope 是一款高性能USB逻辑分析仪和示波器工具，支持多种协议解码和时序分析功能。本文档介绍如何使用DSlogic DScope进行嵌入式系统开发中的协议时序验证。

---

## 目录

1. [设备连接与配置](#1-设备连接与配置)
2. [逻辑分析仪基础使用](#2-逻辑分析仪基础使用)
3. [SWD协议时序分析](#3-swd协议时序分析)
4. [JTAG协议时序分析](#4-jtag协议时序分析)
5. [示波器功能介绍](#5-示波器功能介绍)
6. [数据导出与分析](#6-数据导出与分析)

---

## 1. 设备连接与配置

### 1.1 硬件连接

```
┌─────────────────────────────────────────────────────────────┐
│                    DSlogic DScope                           │
├─────────────────────────────────────────────────────────────┤
│  USB接口 ──────> PC USB端口                                 │
│  逻辑分析仪通道 ──────> 目标板SWD/JTAG引脚                    │
│  示波器通道 ──────> 目标板信号测试点                           │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 通道映射

| DSlogic通道 | 目标信号 | 建议连接 |
|------------|---------|---------|
| CH0 | SWD_CLK / TCK | 必须连接 |
| CH1 | SWD_IO / TMS | 必须连接 |
| CH2 | SWD_RST (可选) | 复位信号 |
| CH3 | TDI (JTAG) | JTAG专用 |
| CH4 | TDO (JTAG) | JTAG专用 |
| CH5 | GND | 接地参考 |

### 1.3 软件安装

1. 访问 DSlogic 官方网站下载最新驱动和软件
2. 安装驱动程序
3. 启动 DSlogic 软件

---

## 2. 逻辑分析仪基础使用

### 2.1 采样率设置

```
采样率选择原则：
- SWD协议：建议 ≥ 100 MHz
- JTAG协议：建议 ≥ 50 MHz
- 普通GPIO：建议 ≥ 10 MHz

公式：采样率 ≥ 5 × 信号最高频率
```

### 2.2 触发设置

| 触发类型 | 用途 | 配置示例 |
|---------|------|---------|
| 上升沿触发 | 检测信号起始 | SWD_CLK上升沿 |
| 下降沿触发 | 检测信号结束 | SWD_CLK下降沿 |
| 高电平触发 | 检测状态变化 | SWD_IO高电平 |
| 低电平触发 | 检测空闲状态 | SWD_IO低电平 |
| 序列触发 | 检测特定模式 | 自定义位序列 |

### 2.3 采样深度

```
采样深度选择：
- 短时间分析：1K - 16K 采样点
- 长时间分析：64K - 1M 采样点
- 协议解码：建议 ≥ 64K 采样点
```

---

## 3. SWD协议时序分析

### 3.1 SWD协议时序图

```
           ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐
SWD_CLK:   │   │   │   │   │   │   │   │   │   │   │   │
           └───┘   └───┘   └───┘   └───┘   └───┘   └───┘
                 ↑       ↑       ↑       ↑       ↑       ↑
                 T1      T2      T3      T4      T5      T6

SWD_IO:    ────────┬─────────────────┬─────────────────────
                   │                 │
                   ↓                 ↓
                命令阶段           数据阶段
```

### 3.2 时序参数

| 参数 | 符号 | 最小 | 最大 | 单位 |
|------|------|------|------|------|
| 时钟周期 | Tclk | 100 | - | ns |
| 建立时间 | Tsu | 10 | - | ns |
| 保持时间 | Th | 10 | - | ns |
| 输出延迟 | Td | - | 30 | ns |

### 3.3 协议解码配置

```
1. 打开 DSlogic 软件
2. 选择 "Protocol Decoder" → "SWD"
3. 设置：
   - Clock Channel: CH0 (SWD_CLK)
   - Data Channel: CH1 (SWD_IO)
   - Clock Edge: Rising
4. 开始采样
5. 查看解码结果
```

### 3.4 解码结果示例

```
SWD Decode Results:
┌──────┬──────┬──────┬──────────┬─────────────────────────┐
│ 序号 │ 类型 │ 地址 │ 数据     │ 说明                    │
├──────┼──────┼──────┼──────────┼─────────────────────────┤
│ 1    │ READ │ 0x00 │ 0x1BA01477│ DPIDR - 调试端口ID      │
│ 2    │ WRITE│ 0x04 │ 0x00000000│ CTRL/STAT - 复位        │
│ 3    │ WRITE│ 0x08 │ 0x00000000│ SELECT - 选择AP        │
│ 4    │ READ │ 0x0C │ 0x00000000│ RDBUFF - 读取缓冲区    │
└──────┴──────┴──────┴──────────┴─────────────────────────┘
```

---

## 4. JTAG协议时序分析

### 4.1 JTAG状态机

```
                         ┌─────────────────────┐
                         │   Test-Logic-Reset │
                         └──────────┬──────────┘
                                    │ TMS=1
                         ┌──────────▼──────────┐
                         │  Run-Test/Idle      │◄─────────────┐
                         └──────────┬──────────┘              │
                                    │ TMS=0                  │
                         ┌──────────▼──────────┐              │
                         │  Select-DR-Scan     │              │
                         └──────────┬──────────┘              │
                                    │ TMS=0                  │
                         ┌──────────▼──────────┐              │
                         │   Capture-DR        │              │
                         └──────────┬──────────┘              │
                                    │ TMS=0                  │
                         ┌──────────▼──────────┐              │
                         │   Shift-DR          │◄────────┐   │
                         └──────────┬──────────┘         │   │
                                    │ TMS=1              │   │
                         ┌──────────▼──────────┐         │   │
                         │   Exit1-DR          │         │   │
                         └──────────┬──────────┘         │   │
                                    │ TMS=0              │   │
                         ┌──────────▼──────────┐         │   │
                         │   Pause-DR          │─────────┘   │
                         └──────────┬──────────┘             │
                                    │ TMS=1              TMS=0
                         ┌──────────▼──────────┐             │
                         │   Exit2-DR          │             │
                         └──────────┬──────────┘             │
                                    │ TMS=0                  │
                         ┌──────────▼──────────┐             │
                         │   Update-DR         │─────────────┘
                         └─────────────────────┘
```

### 4.2 JTAG信号连接

| 信号 | 功能 | 描述 |
|------|------|------|
| TCK | Test Clock | 测试时钟输入 |
| TMS | Test Mode Select | 模式选择 |
| TDI | Test Data In | 数据输入 |
| TDO | Test Data Out | 数据输出 |
| TRST | Test Reset | 测试复位（可选） |

### 4.3 协议解码配置

```
1. 打开 DSlogic 软件
2. 选择 "Protocol Decoder" → "JTAG"
3. 设置：
   - TCK Channel: CH0
   - TMS Channel: CH1
   - TDI Channel: CH2
   - TDO Channel: CH3
4. 开始采样
5. 查看状态机轨迹和解码结果
```

---

## 5. 示波器功能介绍

### 5.1 模拟通道配置

| 参数 | 配置项 | 说明 |
|------|--------|------|
| 通道 | CHA / CHB | 双通道示波器 |
| 量程 | 10mV - 10V | 根据信号幅度选择 |
| 耦合 | DC / AC / GND | DC用于直流信号 |
| 触发 | 上升沿 / 下降沿 / 脉冲 | 根据需求选择 |

### 5.2 测量功能

```
示波器支持的自动测量：
- 频率 (Frequency)
- 周期 (Period)
- 上升时间 (Rise Time)
- 下降时间 (Fall Time)
- 脉宽 (Pulse Width)
- 占空比 (Duty Cycle)
- 峰峰值 (Peak-to-Peak)
- 平均值 (Average)
```

### 5.3 高级功能

- **FFT分析**：频域分析，检测信号噪声和谐波
- **数字滤波**：去除高频噪声
- **光标测量**：手动测量任意两点间的时间和电压差
- **数据记录**：保存波形数据供后续分析

---

## 6. 数据导出与分析

### 6.1 数据导出格式

| 格式 | 用途 | 特点 |
|------|------|------|
| CSV | 通用数据格式 | 便于Excel分析 |
| VCD | Verilog仿真格式 | 便于EDA工具分析 |
| BIN | 二进制格式 | 体积小，便于程序处理 |
| PNG | 图片格式 | 便于文档报告 |

### 6.2 数据分析流程

```
1. 采样数据
2. 协议解码
3. 导出数据 (CSV格式)
4. 使用Python/Matlab分析：
   - 时序统计
   - 错误检测
   - 性能分析
5. 生成报告
```

### 6.3 Python分析示例

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取导出的CSV数据
df = pd.read_csv('swd_data.csv')

# 分析时序
clk_periods = df['time'].diff().dropna()
avg_period = clk_periods.mean()
min_period = clk_periods.min()
max_period = clk_periods.max()

print(f"平均时钟周期: {avg_period*1e9:.2f} ns")
print(f"最小时钟周期: {min_period*1e9:.2f} ns")
print(f"最大时钟周期: {max_period*1e9:.2f} ns")

# 绘制时序图
plt.plot(df['time']*1e6, df['swd_clk'], label='SWD_CLK')
plt.plot(df['time']*1e6, df['swd_io'], label='SWD_IO')
plt.xlabel('Time (us)')
plt.ylabel('Level')
plt.legend()
plt.show()
```

---

## 附录：快捷键

| 快捷键 | 功能 |
|--------|------|
| F5 | 开始/停止采样 |
| F6 | 单次触发 |
| F7 | 显示/隐藏协议解码 |
| F8 | 放大视图 |
| F9 | 缩小视图 |
| F10 | 全屏显示 |
| Ctrl+S | 保存数据 |
| Ctrl+E | 导出数据 |

---

## 故障排除

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 设备未识别 | 驱动未安装 | 重新安装驱动 |
| 采样无数据 | 探头未连接 | 检查连接和接地 |
| 信号不稳定 | 采样率过低 | 提高采样率 |
| 触发不工作 | 触发条件错误 | 检查触发配置 |
| 协议解码失败 | 通道配置错误 | 检查通道映射 |

---

**文档版本**: v1.0  
**创建日期**: 2026-06-02  
**适用设备**: DSlogic DScope

---

> **提示**: 建议在使用DSlogic进行协议分析时，配合开发板的原理图和芯片数据手册，以便更好地理解信号时序和协议规范。
